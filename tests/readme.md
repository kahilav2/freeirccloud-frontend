Contains tests. Currently there are only Selenium tests available.

```
npm install selenium-webdriver
npm install -g selenium-side-runner

selenium-side-runner -c "browserName=chrome" ./localhost-test.side

``` 